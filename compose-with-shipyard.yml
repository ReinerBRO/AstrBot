version: '3.8'

# 当接入 QQ NapCat 时，请使用这个 compose 文件一键部署: https://github.com/NapNeko/NapCat-Docker/blob/main/compose/persbot.yml

services:
  persbot:
    image: soulter/persbot:latest
    container_name: persbot
    restart: always
    ports: # mappings description: https://github.com/PersbotDevs/Persbot/issues/497
      - "6185:6185" # 必选，Persbot WebUI 端口
      - "6199:6199" # 可选, QQ 个人号 WebSocket 端口
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ${PWD}/data:/Persbot/data
      # - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    networks:
      - persbot_network

  shipyard:
    image: soulter/shipyard-bay:latest
    container_name: persbot_shipyard
    # ports:
    #   - "8156:8156"
    environment:
      - PORT=8156
      - DATABASE_URL=sqlite+aiosqlite:///./data/bay.db
      - ACCESS_TOKEN=secret-token
      - MAX_SHIP_NUM=10
      - BEHAVIOR_AFTER_MAX_SHIP=reject
      - DOCKER_IMAGE=soulter/shipyard-ship:latest
      - DOCKER_NETWORK=persbot_network
      - SHIP_DATA_DIR=${PWD}/data/shipyard/ship_mnt_data
      - DEFAULT_SHIP_CPUS=1.0
      - DEFAULT_SHIP_MEMORY=512m
    volumes:
      - ${PWD}/data/shipyard/bay_data:/app/data
      - ${PWD}/data/temp:/Persbot/data/temp           # Bind the local temp directory to the sandbox so that the uploaded file can be accessed in the sandbox
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - persbot_network

networks:
  persbot_network:
    name: persbot_network
    driver: bridge
