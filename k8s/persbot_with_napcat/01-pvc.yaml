apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: persbot-data-shared-pvc
  namespace: persbot-ns
  labels:
    app: persbot-stack
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  # storageClassName: nfs-client # Uncomment and set your RWX storage class if needed

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-config-pvc
  namespace: persbot-ns
  labels:
    app: persbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-qq-pvc
  namespace: persbot-ns
  labels:
    app: persbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard

---
# 持久化 machine-id，保持设备标识不变
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: napcat-machine-id-pvc
  namespace: persbot-ns
  labels:
    app: persbot-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi  # 只需存储一个 32 字节的文件
  # storageClassName: standard