# 日志卡片视图后端测试报告（Agent A）

测试日期：2026-02-11  
范围：`app/service.py` 卡片方法、`app/web.py` 卡片路由、回归测试

## 1. 测试命令

```bash
python -m unittest discover -s tests -v
python -m compileall app watcher
```

## 2. 结果摘要

- 单元/集成测试：通过
  - 总计 32 项，全部通过
- 语法编译检查：通过

## 3. 新增测试项

### `tests/test_cards_service.py`

- `test_cards_data_filters_and_pagination`
- `test_cards_data_search_and_meta`
- `test_update_card`
- `test_delete_card_marks_tree_deleted_when_orphan`
- `test_export_cards_formats`

覆盖点：

- 卡片筛选（分类）、排序、分页
- 搜索与元信息解析（`source/vl/event/confidence`）
- 更新卡片摘要和分类
- 删除卡片及孤儿树元数据处理
- 导出格式（Markdown/JSON/CSV）

### `tests/test_cards_api.py`

- `test_get_cards_success`
- `test_get_cards_invalid_page`
- `test_put_delete_and_export`

覆盖点：

- 新路由参数解析与调用链
- 错误参数返回 400
- 导出接口响应头 `Content-Disposition`

## 4. 已知限制

- `event_data` 当前为占位结构（固定值），未对接真实事件统计。
- 当前搜索为简单子串匹配，不支持正则与高亮。

## 5. 结论

Agent A 交付范围（后端 4 方法、4 路由、测试与文档）已完成并通过现有测试回归。

